.page-header
  .pull-right
    - if not action_is? :index
      = link_to resource_path, :class => 'btn' do
        %i.icon-arrow-left.icon-white
        Back

  %h1= Resource.model_name.human.titleize.pluralize
  
= flash_messages