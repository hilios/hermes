= simple_form_for([@website, @resource]) do |f|
  = f.error_notification
  = f.fields_for :asset do |asset_form|
    = asset_form.input :_type, :as => :hidden
    = render("resources/#{asset_type.underscore}_fields", :f => asset_form)
  .form-actions
    = f.button :submit
