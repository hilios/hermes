= simple_form_for(@website) do |f|
  = f.error_notification
  = f.input :name
  .form-actions
    = f.button :submit
    = link_to websites_path, :class => 'btn' do
      %i.icon-arrow-left
      Back
